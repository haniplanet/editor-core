import * as React from 'react';
import { Editor } from '@atlaskit/editor-core';

const MasterTemp: React.FC = React.memo(() => {
  return (
    <div>
      <Editor />
    </div>
  );
});

export default MasterTemp;
